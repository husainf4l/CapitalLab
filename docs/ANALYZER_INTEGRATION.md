# Analyzer Integration

Analyzer imports are modeled as analyzer, import batch, and result rows. Production validation should include a real analyzer sample file, malformed import handling, duplicate import behavior, and audit traces.
